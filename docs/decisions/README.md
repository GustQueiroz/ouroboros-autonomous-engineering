# Architecture Decision Records

This directory holds numbered ADRs.

Each ADR resolves a DEFINIR item, a specification ambiguity, or a substantive architecture decision. Naming: `NNNN-slug.md`, starting at `0001-`.

Empty by design at bootstrap: no DEFINIR item has been resolved yet.
